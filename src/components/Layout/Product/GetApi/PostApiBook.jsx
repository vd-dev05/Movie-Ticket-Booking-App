export const PostData = (data) => ({
    description: data.description,
    id: data.id,
    poster: data.poster,
    title: data.title,
    theFirm: data.theFirm,
    type: data.type,
    language: data.language,
    total: 0,
    timeBook: 0,
    dateBook: 0,
    seatBook: '',
});